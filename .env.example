# Frontend (React / CRA) — questi finiscono nel bundle browser.
# SOLO chiavi pubbliche. Mai mettere qui la service-role key.
# In locale: copia in .env.local. In produzione: impostale su Vercel.

REACT_APP_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOi...   # anon/public key (safe nel browser)
REACT_APP_API_URL=http://localhost:10000    # backend; in prod = URL Railway
